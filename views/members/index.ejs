<%- include('../partials/header') %>

<div class="page-header">
  <h1>รายการสมาชิก</h1>
  <a href="/members/new" class="btn btn-primary no-print">➕ เพิ่มสมาชิก</a>
</div>

<!-- Search and Pagination Controls -->
<div class="controls no-print">
  <form class="search-form" method="GET" action="/members">
    <input type="text" name="search" placeholder="ค้นหาชื่อ, อีเมล หรือเบอร์โทร..." value="<%= search %>">
    <button type="submit" class="btn btn-secondary">🔍 ค้นหา</button>
    <% if (search) { %>
      <a href="/members" class="btn btn-light">ล้าง</a>
    <% } %>
  </form>
  
  <div class="limit-selector">
    <label>แสดง:</label>
    <select onchange="window.location.href='?page=1&search=<%= search %>&limit='+this.value">
      <option value="10" <%= pagination.limit === 10 ? 'selected' : '' %>>10</option>
      <option value="25" <%= pagination.limit === 25 ? 'selected' : '' %>>25</option>
      <option value="50" <%= pagination.limit === 50 ? 'selected' : '' %>>50</option>
    </select>
    <span>รายการต่อหน้า</span>
  </div>
</div>

<!-- Print Header (hidden on screen) -->
<div class="print-header" style="display: none;">
  <h2>รายงานรายการสมาชิก</h2>
  <p>วันที่พิมพ์: <%= new Date().toLocaleDateString('th-TH') %></p>
  <p>จำนวนทั้งหมด: <%= pagination.totalItems || 0 %> รายการ</p>
</div>

<!-- Members Table -->
<% if (members && members.length > 0) { %>
  <table class="data-table">
    <thead>
      <tr>
        <th>#</th>
        <th>ชื่อ-นามสกุล</th>
        <th>อีเมล</th>
        <th>เบอร์โทรศัพท์</th>
        <th>วันที่สมัคร</th>
        <th class="no-print actions-column">จัดการ</th>
      </tr>
    </thead>
    <tbody>
      <% members.forEach((member, index) => { %>
        <tr>
          <td><%= (pagination.page - 1) * pagination.limit + index + 1 %></td>
          <td><%= member.full_name %></td>
          <td><%= member.email %></td>
          <td><%= member.phone_number %></td>
          <td><%= new Date(member.joined_date).toLocaleDateString('th-TH') %></td>
          <td class="no-print actions-column">
            <a href="/members/<%= member.id %>/edit" class="btn btn-sm btn-warning">แก้ไข</a>
            <form action="/members/<%= member.id %>/delete" method="POST" onsubmit="return confirm('คุณแน่ใจหรือไม่ที่จะลบสมาชิกนี้?')">
              <button type="submit" class="btn btn-sm btn-danger">ลบ</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
  
  <!-- Pagination -->
  <% if (pagination.totalPages > 1) { %>
    <div class="pagination no-print">
      <% if (pagination.hasPrev) { %>
        <a href="?page=<%= pagination.page - 1 %>&search=<%= search %>&limit=<%= pagination.limit %>" class="btn btn-light">← ก่อนหน้า</a>
      <% } %>
      
      <span class="page-info">หน้า <%= pagination.page %> จาก <%= pagination.totalPages %> (ทั้งหมด <%= pagination.totalItems %> รายการ)</span>
      
      <% if (pagination.hasNext) { %>
        <a href="?page=<%= pagination.page + 1 %>&search=<%= search %>&limit=<%= pagination.limit %>" class="btn btn-light">ถัดไป →</a>
      <% } %>
    </div>
  <% } %>
<% } else { %>
  <p class="no-data">ไม่พบข้อมูลสมาชิก</p>
<% } %>

<!-- Print Button -->
<button onclick="window.print()" class="btn btn-secondary no-print" style="margin-top: 20px;">🖨️ พิมพ์รายงาน</button>

<%- include('../partials/footer') %>
